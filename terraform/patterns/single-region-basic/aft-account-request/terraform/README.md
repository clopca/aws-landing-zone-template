# AFT Account Request Example

This pattern no longer ships a runnable Terraform root in this directory.

Use the canonical examples in `terraform/aft/account-requests/*.tf.example` for account-request definitions.
